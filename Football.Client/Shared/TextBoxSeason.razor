@if (!string.IsNullOrWhiteSpace(Label))
{
    <label for="@Id">
        @Label
    </label>
}
<div class="input-group mb-3 ">
    <input type="text" class="form-control" placeholder="Season..." id="@Id" value="@Season" @onchange="this.OnChange">
</div>

@code {
    [Parameter] public String Season { get; set; } = " ";
    [Parameter] public EventCallback<string> SeasonChanged { get; set; }
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? Id { get; set; }
    [Parameter] public string? Placeholder { get; set; }

    private void OnChange(ChangeEventArgs e)
    {
        SeasonChanged.InvokeAsync(e.Value.ToString());
    }
}
