@if (!string.IsNullOrWhiteSpace(Label))
{
    <label for="@Id">
        @Label
    </label>
}
<div class="input-group mb-3 ">
    <input type="text" class="form-control" placeholder="Week..." id="@Id" value="@Week" @onchange="this.OnChange">
</div>

@code {
    [Parameter] public String Week { get; set; } = " ";
    [Parameter] public EventCallback<string> WeekChanged { get; set; }
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? Id { get; set; }
    [Parameter] public string? Placeholder { get; set; }

    private void OnChange(ChangeEventArgs e)
    {
        WeekChanged.InvokeAsync(e.Value.ToString());
    }
}
