@if (!string.IsNullOrWhiteSpace(Label))
{
    <label for="@Id">
        @Label
    </label>
}
<div class="input-group mb-3 ">
    <input type="text" class="form-control" placeholder="Number of Teams..." id="@Id" value="@TeamCount" @onchange="this.OnChange">
</div>

@code {
    [Parameter] public String TeamCount { get; set; } = " ";
    [Parameter] public EventCallback<string> TeamCountChanged { get; set; }
    [Parameter] public string? Label { get; set; }
    [Parameter] public string? Id { get; set; }
    [Parameter] public string? Placeholder { get; set; }

    private void OnChange(ChangeEventArgs e)
    {
        TeamCountChanged.InvokeAsync(e.Value.ToString());
    }
}

